apiVersion: v1
kind: Service
metadata:
  name: backstage-backend
  namespace: backstage
spec:
  type: ClusterIP
  selector:
    app: backstage-backend
  ports:
    - name: http
      port: 7007
      targetPort: http
